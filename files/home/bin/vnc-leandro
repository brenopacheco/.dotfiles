#!/bin/bash
# Open x11vnc

PORT=$1
if [[ -z "$PORT" ]]; then
	echo "Usage: vnc-leandro <PORT>"
	exit 1
fi

function open_viewer() {
  sleep 10
  vncviewer localhost:5900
}

open_viewer &

ssh -i ~/.ssh/leandro_rsa leandro@0.tcp.sa.ngrok.io -p "$PORT" -L 5900:localhost:5900 'x11vnc -display :1'

