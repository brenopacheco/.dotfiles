(import-macros {: any?} :macros)

(local buf (require :utils.buf) )

; (vim.notify (vim.inspect buf))

; (import-macros foo :foo)
; (local bar (require :bar) )

; (vim.notify (buf.uuid))

; (vim.print (any? x [1 2 3] (= x 2)))
