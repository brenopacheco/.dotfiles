[general]
live_config_reload = true
import = [ "~/.config/alacritty/themes/nightfly-alacritty.toml" ]

[window]
dynamic_padding = true

[font]
size        = 11.0
normal      = { family = "FiraCode Nerd Font Mono", style = "Regular" }
bold        = { family = "FiraCode Nerd Font Mono", style = "Bold"    }
italic      = { family = "FiraCode Nerd Font Mono", style = "Regular" }
bold_italic = { family = "FiraCode Nerd Font Mono", style = "Bold"    }

[keyboard]
bindings = [
  # NOTE: removed the manual CSI u bindings (Ctrl+1..9, Ctrl+Backspace, etc.)
  # — they were a workaround for tmux's old key handling.  Tmux now has
  # `extkeys` + `csi-u` and Alacritty sends correct sequences natively.
  { key = "M", mods = "Control", chars = "\u001b[109;5u" },
  { key = "Equals", mods = "Super", action = "IncreaseFontSize" },
  { key = "Minus",  mods = "Super", action = "DecreaseFontSize" },
]
