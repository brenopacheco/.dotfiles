# Switch to tab <num> with `1gt`, `2gt`, ..., `9gt`
# for i in range(1, 10):
#     config.bind(f'{i}gt', f'view-switch {i-1}')

# Use `Ctrl+N` and `Ctrl+P` to go to the next and previous tab (similar to tabbing)
# config.bind('<c-n>', 'tab-next')
# config.bind('<c-p>', 'tab-prev')

# Close tab with 'x'
config.bind('x', 'tab-close')
